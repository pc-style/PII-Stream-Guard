// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Update to the final domain before deploy (Vercel / custom subdomain).
  site: 'https://piistreamguard.pcstyle.dev',
});
